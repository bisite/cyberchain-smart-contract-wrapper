[**@cyberchain/smart-contract-wrapper**](../README.md)

***

[@cyberchain/smart-contract-wrapper](../README.md) / bigintsToHex

# Function: bigintsToHex()

> **bigintsToHex**(`o`): `unknown`

Defined in: src/utils.ts:226

Turns bigint elements into hex strings to allow for JSON serialization

## Parameters

### o

`unknown`

The object

## Returns

`unknown`

The same object, but all the bigints turned into hex strings with 0x prefix
